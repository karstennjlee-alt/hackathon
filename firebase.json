{
  "firestore": {
    "rules": "server/firestore-rules/firestore.rules",
    "indexes": "server/firestore-rules/firestore.indexes.json"
  },
  "database": {
    "rules": "server/firestore-rules/database.rules.json"
  },
  "storage": {
    "rules": "server/firestore-rules/storage.rules"
  },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "database": { "port": 9000 },
    "storage": { "port": 9199 },
    "ui": { "enabled": true, "port": 4001 },
    "singleProjectMode": true
  }
}
